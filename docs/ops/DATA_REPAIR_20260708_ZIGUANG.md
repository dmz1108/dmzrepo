# 数据修复方案:2026-07-08 算力AI 龙头池补入紫光股份

状态:待 Codex 审核 → Codex 云端执行 → 双方验证
Owner 决定:方案2(修数据本身,统计按真实龙头计)。理由:系统尚未到对外公信力阶段,且本案不是策略错误,是采集设计疏漏导致真龙头缺席;修正应让策略页展示并统计真实龙头。

## 背景

- 7-08 当日快照采集不完整,紫光股份(000938)未进入 `cardData` 的 `zt10/gain10/gain30` 统计表,龙头评分池里没有它。
- 龙头评分 v2(PR #9,已上线)当时已核定:**紫光股份 90 分第一龙头、长源东谷 79 分第二**(双源核验:腾讯/东财互证,10日 +21.55 / 30日 +16.6 / 主因 07-06 算力/云计算数据中心 / zt10 主次数 2)。
- v2 代码只能在池内排名,历史日重算补不出缺失的源数据 → 须修复快照文件本身。线上实测 7-08 响应通篇无紫光,龙头仍为 长源东谷/航锦科技/冰轮环境。

## 修复方式

脚本:`tools/patch-20260708-suanli-leaders.js`(本 PR 附带,已在合成快照上测试)。

- 锚点定位:仅修补「长源东谷(603950) 出现在 zt10/gain10/gain30 任一表」的板块(算力AI 龙头池所在板块),避免误伤无关板块;
- 补入内容:三张表各补一行紫光股份,数值即上面的双源核验数字;**不动 ztList**(紫光当日未涨停,不伪造涨停记录);
- 安全性:默认 dry-run 只打印;`--apply` 先备份原文件(`.bak-时间戳`)再整文件原子替换;幂等(已存在即跳过);
- 无需重启任何服务(快照文件按请求读取)。

## 执行步骤(Codex,云端)

1. 仓库目录下 `node tools/patch-20260708-suanli-leaders.js` 看 dry-run 输出,确认命中的板块合理(应为算力/云计算类板块,zs6/5/7 视快照存在情况);
2. 无异议后 `node tools/patch-20260708-suanli-leaders.js --apply`;
3. 验证:`GET /api/strategy-mainlines?day=2026-07-08` → 算力AI 龙头应为 紫光股份 第一、长源东谷 第二;
4. 若补数后紫光仍未上榜:说明历史日龙头池构建另有入口未吃 cardData,把 dry-run/验证输出发回,Claude 跟进代码侧;
5. 执行结果(含备份文件名)记入云端操作日志。

## 回滚

`cp <快照>.bak-<时间戳> <快照>` 即回滚,无其他副作用。

## 统计口径说明

修复后,预判回看/复盘统计自然按修正数据计算(统计每次从数据现算,无缓存需清)。7-08 一案同时作为「采集完整性疏漏」的记录案例:v2 的池子补全已覆盖后续交易日,本修复仅为追溯。
