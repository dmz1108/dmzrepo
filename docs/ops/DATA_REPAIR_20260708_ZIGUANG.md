# 数据修复方案:2026-07-08 算力AI 龙头池补入星网锐捷 + 紫光股份

状态:待 Codex 审核 → Codex 云端执行 → 双方验证
Owner 决定:方案2(修数据本身,统计按真实龙头计)。理由:系统尚未到对外公信力阶段,且本案不是策略错误,是采集设计疏漏导致真龙头缺席;修正应让策略页展示并统计真实龙头。
Owner 核定最终龙头:**2 只——星网锐捷(002396) 第一、紫光股份(000938) 第二**(v2 全量池评分 86/73)。

## 背景与修正目标溯源

- 早期"紫光 90 / 长源东谷 79"是 Claude 手工残缺池(旧页面 leaders+大涨名单拼的 7 只)算出的中间结论,**已被推翻**:星网锐捷当时被该手工池漏掉。
- v2(PR #9)+ 池子补全上线后的系统全量池终稿:**星网锐捷 86 分主龙头**(近10日 5 板:06-30/07-01/07-03/07-06/07-08,10日+54.87% 第1,30日+22.97),**紫光股份 73 分第二**(2 板,10日+21.55,30日+16.6,双源核验),长源东谷掉出前二。
- 7-08 存档诊断(线上实测):
  1. 紫光股份整份响应缺席——cardData zt10/gain10/gain30 无它;
  2. 星网锐捷当日真实涨停(复盘库主因=算力/CPO交换机),却只挂在网络安全类板块,算力板块 ztList/统计表均无它,且其存档 kline 统计为 0;
  3. 因此算力AI 龙头仍显示 长源东谷/航锦科技/冰轮环境(长源 basis 还带着错误的"今日2板")。

## 修复方式

脚本:`tools/patch-20260708-suanli-leaders.js`(本 PR 附带,已在合成快照上测试双股全流程)。

- 锚点定位:仅修补「长源东谷(603950) 出现在 zt10/gain10/gain30 任一表」的板块(算力AI 龙头池所在板块);
- 补入:两只股各补三张统计表(数值见脚本 PATCHES,均为核验值);
- **ztList 只补星网锐捷**——它 7-08 真实涨停,进 todayCodes 是事实还原;紫光当日未涨停,不进 ztList,不伪造涨停记录;
- 星网锐捷补入算力板块**不删除**它在网络安全板块的既有归属(一股双主线是真实现象;题材族边界归后续语义讨论);
- 安全性:默认 dry-run;`--apply` 先备份(`.bak-时间戳`)再整文件原子替换;幂等;
- 无需重启任何服务(快照文件按请求读取)。

## 执行步骤(Codex,云端)

1. `node tools/patch-20260708-suanli-leaders.js` 看 dry-run,确认命中板块合理(算力/云计算类,zs6/5/7 视快照存在);
2. 无异议后加 `--apply` 执行;
3. 验证:`GET /api/strategy-mainlines?day=2026-07-08` → 算力AI 龙头 = **星网锐捷 第一、紫光股份 第二**;
4. 若排名仍不符:说明历史日龙头池另有入口未吃 cardData/ztList(或主线归属映射把 002396 的涨停留在网络安全),把 dry-run 与验证输出发回,Claude 跟进代码侧;
5. 执行结果(含备份文件名)记入云端操作日志。

## 回滚

`cp <快照>.bak-<时间戳> <快照>` 即回滚,无其他副作用。

## 统计口径说明

修复后,预判回看/复盘统计自然按修正数据计算(统计每次从数据现算,无缓存需清)。本案同时作为两类疏漏的记录案例:①采集完整性(紫光缺席);②涨停归因把双主线股只记单边(星网只进网络安全)。v2 池子补全已覆盖后续交易日的①;②属题材族边界议题,后续语义讨论解决。
